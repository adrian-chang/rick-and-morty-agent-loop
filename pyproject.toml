[project]
name = "rick-and-morty-agent-loop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic-ai-slim[anthropic,openai,openrouter]>=2.46.0",
    "python-dotenv>=1.2.3",
    "ramapi>=0.1.0",
]

[project.scripts]
rick-and-morty-agent-loop = "rick_and_morty_agent_loop.main:main"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
